# Runtime-only dependencies for the AI microservice container.
# Dev/test/lint/docs/notebooks viven en requirements-dev.txt y NO se instalan en
# la imagen de producción. Esto baja el venv de ~3 GB a ~900 MB.
#
# Notas:
# - torch se instala en variante CPU-only desde el índice de PyTorch (ver Dockerfile).
#   No fijar versión aquí: el Dockerfile pasa --extra-index-url y la wheel cp311
#   correcta (~200 MB en vez de ~800 MB con CUDA).
# - transformers usa torch para LocalEmbeddingService (lazy-loaded).
# - psycopg2-binary trae libpq estático, sin necesidad de libpq-dev en el host.

# Web framework
fastapi>=0.115.6,<1
uvicorn[standard]>=0.32.0,<1

# Data validation
pydantic>=2.11.0,<3
pydantic-settings>=2.8.0,<3

# Database (PostgreSQL/RDS + SQLite local)
sqlalchemy==2.0.23
psycopg2-binary>=2.9.9,<3

# HTTP client (Ollama + LLM compatibles)
httpx==0.25.2

# ML baseline (entrenamiento de pickles al build y/o entrypoint)
numpy>=2.1.0,<3
scikit-learn>=1.5.2,<2

# Embeddings locales (lazy en LocalEmbeddingService — fallan grácil si faltan).
# transformers>=4.48.0: CVE-2024-11392/11393/11394.
# torch>=2.6.0: CVE-2025-32434, CVE-2024-31580/31583. Variante CPU vía Dockerfile.
torch>=2.6.0,<3
transformers>=4.48.0,<5
