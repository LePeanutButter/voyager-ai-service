"""HTTP API layer: FastAPI routers and dependencies.

Responsibilities:
    Group code that exposes REST endpoints and resolves injectable services
    (`Depends`) from application state.

Dependencies:
    FastAPI, Starlette (`Request`), and `app.api.v1` subpackages.
"""
