"""Version 1 contract for the REST API (`/api/v1`).

Responsibilities:
    Mark the namespace of versioned routers mounted under the common prefix.

Dependencies:
    Concrete routers live in subpackages (`chat`, `users`, etc.).
"""
