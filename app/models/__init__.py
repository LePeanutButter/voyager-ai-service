"""Shared models and schemas at the `models` package level.

Responsibilities:
    Re-export or group legacy or cross-cutting Pydantic definitions.

Dependencies:
    `app.models.schemas` and domain modules under `app.modules`.
"""
