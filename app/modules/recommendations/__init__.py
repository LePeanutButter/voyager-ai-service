"""Personalized activity and destination recommendations.

Responsibilities:
    Curated catalog, scoring with models and rules, endpoints consumed via v1 router.

Dependencies:
    `app.ml.model_loader`, optional `app.modules.trends.service`, local Pydantic schemas.
"""
