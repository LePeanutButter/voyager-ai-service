"""Adaptive UI: menu and home feed from user signals.

Responsibilities:
    Build menu and home feed layouts from preferences and behavior.

Dependencies:
    Local `schemas` and `service`; user data via domain services.
"""
