"""Compatible traveler matching (scores, connections, feedback).

Responsibilities:
    Expose request/response schemas and the matching service.

Dependencies:
    `app.ml.learning_store`, `app.core.config`, ML models via `ModelManager`.
"""
