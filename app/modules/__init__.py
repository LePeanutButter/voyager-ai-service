"""Domain modules: business logic, services, and schemas per bounded context.

Responsibilities:
    Hold recommendations, users, matching, chat, trends, behavior,
    preferences, adaptive UI, and shared utilities (`common`).

Dependencies:
    Pydantic, ML services in `app.ml`, configuration in `app.core.config`.
"""
