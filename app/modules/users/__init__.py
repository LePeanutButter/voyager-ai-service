"""User profile management and interaction history.

Responsibilities:
    Create/update profiles, preferences, interactions, and insights.

Dependencies:
    `app.modules.users.schemas`, `app.modules.users.service`, optional ML via `ModelManager`.
"""
