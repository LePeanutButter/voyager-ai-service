"""Shared utilities and types across domain modules.

Responsibilities:
    Expose reusable enums and base DTOs (location, generic responses, etc.).

Dependencies:
    `app.modules.common.schemas` subpackage.
"""
