"""Adaptive travel preference questionnaire.

Responsibilities:
    In-memory sessions, branching engine, and aggregated profile for the AI engine.

Dependencies:
    `engine`, `service`, and `schemas` in the same package.
"""
